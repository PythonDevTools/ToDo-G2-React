import styled from 'styled-components'

export const StyledApp = styled.div`
    display: flex;

    .sidebar {
        position: fixed;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
`