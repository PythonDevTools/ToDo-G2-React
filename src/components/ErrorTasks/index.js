import { StyledError } from './index.styled'

const ErrorTasks = () => {
    return(
        <StyledError>
            Ups, hay algo mal que no está bien 🙈
        </StyledError>
    )
}

export default ErrorTasks;